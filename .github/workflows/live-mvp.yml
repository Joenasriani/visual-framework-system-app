name: Live MVP Acceptance

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'e2e/live.mjs'
      - 'e2e/complete.mjs'
      - '.github/workflows/live-mvp.yml'

# Final acceptance includes deterministic offline shell verification.
jobs:
  live:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm install --no-save playwright
      - run: npx playwright install --with-deps chromium
      - run: node e2e/live.mjs
        env:
          LIVE_URL: https://visual-framework-system.vercel.app
      - run: node e2e/complete.mjs
        env:
          LIVE_URL: https://visual-framework-system.vercel.app